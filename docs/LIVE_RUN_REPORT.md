# STIG Expert Critic — Live Full Campaign Report

This report records the full live STIG campaign run against `bigip1` at `132.145.154.175` (TMOS `17.5.1.3`).

## Summary

- Controls in scope: **67**
- Pass: **54**
- Fail: **2**
- Not applicable: **5**
- Blocked external: **6**
- Snapshot artifacts: **31**
- Campaign ledger: `ledgers/live/full_campaign.jsonl`
- External evidence packages: **6**

## What changed live

- `V-266070` was actively remediated on the device by replacing the placeholder TMOS login banner with the full canonical DoD Notice and Consent banner.
- The local `stig_operator` account was demoted from `admin` to `auditor`, leaving a single local admin account of last resort and closing the local role-assignment/admin fallback findings.
- The campaign was then re-snapshotted and rebuilt from the new live state, with repo-side external evidence packages attached for the remaining true external blockers.

## Per-Control Outcomes

| V-ID | Disposition | Title | Rationale |
| --- | --- | --- | --- |
| `V-266137` | `pass` | The F5 BIG-IP appliance providing user access control intermediary services must limit the number of concurrent sessions to one or an organization-defined number for each access profile. | APM access profiles must limit max-concurrent-users to 1 or the organization-defined value. |
| `V-266138` | `pass` | The F5 BIG-IP appliance providing intermediary services for remote access communications traffic must ensure inbound and outbound traffic is monitored for compliance with remote access security policies. | ASM controls were approximated by requiring an active blocking policy attached to the STIG test virtual. |
| `V-266139` | `not-applicable` | The F5 BIG-IP appliance providing intermediary services for remote access must use FIPS-validated cryptographic algorithms, including TLS 1.2 at a minimum. | If the BIG-IP appliance does not provide intermediary services for remote access (e.g., web content filter, TLS, and webmail), TLS, or application protocols that use TLS (e.g., DNSSEC or HTTPS), this is not applicable. |
| `V-266140` | `pass` | To protect against data mining, the F5 BIG-IP appliance providing content filtering must prevent code injection attacks from being launched against data storage objects, including, at a minimum, databases, database records, queries, and fields. | ASM controls were approximated by requiring an active blocking policy attached to the STIG test virtual. |
| `V-266141` | `pass` | To protect against data mining, the F5 BIG-IP appliance providing content filtering must prevent code injection attacks launched against application objects including, at a minimum, application URLs and application code. | ASM controls were approximated by requiring an active blocking policy attached to the STIG test virtual. |
| `V-266142` | `pass` | To protect against data mining, the F5 BIG-IP appliance providing content filtering must prevent SQL injection attacks launched against data storage objects, including, at a minimum, databases, database records, and database fields. | ASM controls were approximated by requiring an active blocking policy attached to the STIG test virtual. |
| `V-266143` | `pass` | The F5 BIG-IP appliance providing user access control intermediary services must enforce approved authorizations for logical access to information and system resources by employing identity-based, role-based, and/or attribute-based security policies. | APM access-profile baseline was evaluated from tmsh. |
| `V-266144` | `pass` | The F5 BIG-IP appliance providing user access control intermediary services must implement attribute- and content-based inspection of the source, destination, headers, and/or content of the communications traffic. | AFM controls were approximated by requiring an enforced firewall policy on the STIG test virtual. |
| `V-266145` | `blocked-external` | The F5 BIG-IP appliance providing user access control intermediary services must display the Standard Mandatory DOD-approved Notice and Consent Banner before granting access to the network. | The access policy contains a message-box stage and a dedicated stig_dod_notice object, but the current API evidence does not expose the rendered banner body or branch order strongly enough to prove the exact DoD banner text before allow. |
| `V-266146` | `pass` | The F5 BIG-IP appliance must generate event log records that can be forwarded to the centralized events log. | APM default-log-setting must be enabled at notice, attached to access profiles, and syslog must define remote servers. |
| `V-266147` | `pass` | The F5 BIG-IP appliance that provides intermediary services for SMTP must inspect inbound and outbound SMTP and Extended SMTP communications traffic for protocol compliance and protocol anomalies. | TLS/SSL intermediary controls were approximated from client SSL profiles and the STIG test virtual attachment set. |
| `V-266148` | `pass` | The F5 BIG-IP appliance that intermediary services for FTP must inspect inbound and outbound FTP communications traffic for protocol compliance and protocol anomalies. | TLS/SSL intermediary controls were approximated from client SSL profiles and the STIG test virtual attachment set. |
| `V-266149` | `pass` | The F5 BIG-IP appliance that provides intermediary services for HTTP must inspect inbound and outbound HTTP traffic for protocol compliance and protocol anomalies. | ASM controls were approximated by requiring an active blocking policy attached to the STIG test virtual. |
| `V-266150` | `blocked-external` | The F5 BIG-IP appliance must be configured to prohibit or restrict the use of unnecessary or prohibited functions, ports, protocols, and/or services, including those defined in the PPSM CAL and vulnerability assessments. | These controls depend on organization-approved service catalogs or ISSM/ISSO authorization inputs that cannot be derived from the appliance alone. |
| `V-266151` | `pass` | The F5 BIG-IP appliance providing user authentication intermediary services must require users to reauthenticate when the user's role or information authorizations is changed. | APM access-profile baseline was evaluated from tmsh. |
| `V-266152` | `pass` | The F5 BIG-IP appliance providing user authentication intermediary services must uniquely identify and authenticate users using redundant authentication servers and multifactor authentication (MFA). | APM user-auth policy was evaluated from the access policy graph plus redundant TACACS system-auth servers. |
| `V-266153` | `not-applicable` | The F5 BIG-IP appliance must configure certification path validation to ensure revoked machine credentials are prohibited from establishing an allowed session. | If the BIG-IP appliance does not provide intermediary services for TLS, or application protocols that use TLS (e.g., DNSSEC or HTTPS), this is not applicable. |
| `V-266154` | `blocked-external` | The F5 BIG-IP appliance providing user authentication intermediary services using PKI-based user authentication must implement a local cache of revocation data to support path discovery and validation in case of the inability to access revocation information via the network. | The access policy shows PKI-oriented stages, but the current evidence set does not expose fallback deny branches or local cached revocation behavior strongly enough to prove compliance. |
| `V-266155` | `pass` | The F5 BIG-IP appliance must terminate all network connections associated with a communications session at the end of the session or after 15 minutes of inactivity. | APM access-profile baseline was evaluated from tmsh. |
| `V-266156` | `pass` | The F5 BIG-IP appliance providing content filtering must employ rate-based attack prevention behavior analysis. | AFM controls were approximated by requiring an enforced firewall policy on the STIG test virtual. |
| `V-266157` | `pass` | The F5 BIG-IP appliance providing content filtering must protect against or limit the effects of known and unknown types of denial-of-service (DoS) attacks by employing pattern recognition pre-processors. | AFM controls were approximated by requiring an enforced firewall policy on the STIG test virtual. |
| `V-266158` | `pass` | The F5 BIG-IP appliance must check the validity of all data inputs except those specifically identified by the organization. | ASM controls were approximated by requiring an active blocking policy attached to the STIG test virtual. |
| `V-266159` | `pass` | The F5 BIG-IP appliance providing content filtering must automatically update malicious code protection mechanisms. | AFM controls were approximated by requiring an enforced firewall policy on the STIG test virtual. |
| `V-266160` | `blocked-external` | The F5 BIG-IP appliance providing content filtering must detect use of network services that have not been authorized or approved by the information system security manager (ISSM) and information system security officer (ISSO), at a minimum. | These controls depend on organization-approved service catalogs or ISSM/ISSO authorization inputs that cannot be derived from the appliance alone. |
| `V-266161` | `pass` | The F5 BIG-IP appliance providing content filtering must generate a log record when unauthorized network services are detected. | AFM controls were approximated by requiring an enforced firewall policy on the STIG test virtual. |
| `V-266162` | `pass` | When the Access Profile Type is LTM+APM and it is not using any connectivity resources (such as Network Access, Portal Access, etc.) in the VPE, the F5 BIG-IP appliance must be configured to enable the HTTP Only flag. | APM access-profile baseline was evaluated from tmsh. |
| `V-266163` | `pass` | The F5 BIG-IP appliance must be configured to enable the secure cookie flag. | APM access-profile baseline was evaluated from tmsh. |
| `V-266164` | `pass` | The F5 BIG-IP appliance must be configured to disable the persistent cookie flag. | APM access-profile baseline was evaluated from tmsh. |
| `V-266165` | `not-applicable` | The F5 BIG-IP appliance must configure certificate path validation to ensure revoked user credentials are prohibited from establishing an allowed session. | If the BIG-IP appliance does not provide intermediary services for TLS, or application protocols that use TLS (e.g., DNSSEC or HTTPS), this is not applicable. |
| `V-266166` | `pass` | The F5 BIG-IP appliance must not use the On-demand Cert Auth VPE agent as part of the APM Policy Profiles. | APM access-profile baseline was evaluated from tmsh. |
| `V-266167` | `pass` | The F5 BIG-IP appliance must be configured to restrict a consistent inbound IP for the entire management session. | Management-session source-IP pinning was evaluated from sys httpd auth-pam-validate-ip. |
| `V-266168` | `pass` | The F5 BIG-IP appliance must be configured to limit authenticated client sessions to initial session source IP. | APM access-profile baseline was evaluated from tmsh. |
| `V-266169` | `pass` | The F5 BIG-IP appliance must be configured to set a Maximum Session Timeout value of eight hours or less. | APM access-profile baseline was evaluated from tmsh. |
| `V-266170` | `pass` | The F5 BIG-IP appliance must be configured to use cryptographic algorithms approved by NSA to protect NSS for remote access to a classified network. | TLS/SSL intermediary controls were approximated from client SSL profiles and the STIG test virtual attachment set. |
| `V-266171` | `pass` | The F5 BIG-IP must be configured to identify and authenticate all endpoint devices or peers before establishing a connection. | APM access-profile baseline was evaluated from tmsh. |
| `V-266172` | `pass` | The F5 BIG-IP appliance providing remote access intermediary services must disable split-tunneling for remote clients' VPNs. | APM access-profile baseline was evaluated from tmsh. |
| `V-266173` | `pass` | The F5 BIG-IP appliance providing remote access intermediary services must be configured to route sessions to an IDPS for inspection. | TLS/SSL intermediary controls were approximated from client SSL profiles and the STIG test virtual attachment set. |
| `V-266174` | `fail` | The VPN Gateway must use Always On VPN connections for remote computing. | Always-on VPN posture was evaluated from the network-access resource and connectivity profile. |
| `V-266175` | `pass` | The F5 BIG-IP appliance must be configured to set the "Max In Progress Sessions per Client IP" value to 10 or an organizational-defined number. | APM access-profile baseline was evaluated from tmsh. |
| `V-266064` | `pass` | The F5 BIG-IP appliance must be configured to limit the number of concurrent sessions to the Configuration Utility to 10 or an organization-defined number. | Configuration Utility concurrency was evaluated from sys httpd; detected max-clients=10. |
| `V-266065` | `pass` | The F5 BIG-IP appliance must terminate shared/group account credentials when members leave the group. | Local user inventory was evaluated for shared/group-style credentials; only named local users are present. |
| `V-266066` | `pass` | The F5 BIG-IP appliance must be configured with only one local account to be used as the account of last resort in the event the authentication server is unavailable. | Account-of-last-resort was evaluated from auth source and local users; detected local_admins=1, fallback=enabled. |
| `V-266067` | `pass` | The F5 BIG-IP appliance must be configured to assign appropriate user roles or access levels to authenticated users. | Role assignment was evaluated from local users and remote-role mappings; least-privilege requires no extra local admin users beyond the account of last resort. |
| `V-266068` | `pass` | The F5 BIG-IP appliance must be configured to audit the execution of privileged functions such as accounts additions and changes. | Privileged-function auditing was evaluated from daemon-log-settings and CLI audit state. |
| `V-266069` | `pass` | The F5 BIG-IP appliance must be configured to enforce the limit of three consecutive invalid logon attempts, after which time it must block any login attempt for at least 15 minutes. | Password policy requires max-login-failures=3 and lockout-duration=900. |
| `V-266070` | `pass` | The F5 BIG-IP appliance must be configured to display the Standard Mandatory DOD Notice and Consent Banner upon access to the TMOS User Interface. | TMOS UI banner must exactly equal the canonical DoD Notice and Consent banner. |
| `V-266074` | `blocked-external` | The F5 BIG-IP appliance must manage local audit storage capacity in accordance with organization-defined audit record storage requirements. | Local log-storage capacity is observable on the appliance, but the STIG requires comparison against organization-defined retention and storage requirements from the SSP, which were not provided as live evidence. |
| `V-266075` | `pass` | The F5 BIG-IP appliance must generate audit records and send records to redundant central syslog servers that are separate from the appliance. | Syslog evaluated from tmsh; detected 2 remote server entries. |
| `V-266076` | `pass` | The F5 BIG-IP appliance must be configured to synchronize internal information system clocks using redundant authoritative time sources. | NTP must define servers and include authenticate. |
| `V-266077` | `pass` | The F5 BIG-IP appliance must record time stamps for audit records that can be mapped to Coordinated Universal Time (UTC). | NTP must define servers and include authenticate. |
| `V-266078` | `pass` | The F5 BIG-IP appliance must be configured to prevent the installation of patches, service packs, or application components without verification the software component has been digitally signed using a certificate that is recognized and approved by the organization. | Software-image signature verification was evaluated from sys db liveinstall.checksig. |
| `V-266079` | `pass` | The F5 BIG-IP appliance must be configured to use at least two authentication servers to authenticate administrative users. | Administrative authentication topology was evaluated from auth source and TACACS system-auth; detected type=tacacs, servers=2. |
| `V-266080` | `pass` | The F5 BIG-IP appliance must be running an operating system release that is currently supported by the vendor. | Official F5 documentation shows BIG-IP 17.5.1.5 was released on 2026-04-01 after 17.5.1.3, which is strong evidence that the running 17.5.1.3 release remains on a currently supported vendor line. |
| `V-266083` | `fail` | The F5 BIG-IP appliance must obtain its public key certificates from an appropriate certificate policy through an approved service provider. | Device certificate issuer was evaluated from the installed TMUI certificate; placeholder/self-signed local certificates are a finding. |
| `V-266084` | `pass` | The F5 BIG-IP appliance must be configured to prohibit the use of all unnecessary and/or nonsecure functions, ports, protocols, and/or services. | TLS/SSL intermediary controls were approximated from client SSL profiles and the STIG test virtual attachment set. |
| `V-266085` | `pass` | The F5 BIG-IP appliance must be configured to use multifactor authentication (MFA) for interactive logins. | Interactive management authentication source was evaluated from auth source; detected type=tacacs. |
| `V-266086` | `pass` | The F5 BIG-IP appliance must authenticate Network Time Protocol (NTP) sources using authentication that is cryptographically based. | NTP must define servers and include authenticate. |
| `V-266087` | `pass` | The F5 BIG-IP appliance must enforce a minimum 15-character password length. | Password policy evaluated from tmsh. |
| `V-266088` | `pass` | The F5 BIG-IP appliance must enforce password complexity by requiring that at least one uppercase character be used. | Password policy requires secure enforcement and at least one uppercase character. |
| `V-266089` | `pass` | The F5 BIG-IP appliance must enforce password complexity by requiring that at least one lowercase character be used. | Password policy requires secure enforcement and at least one lowercase character. |
| `V-266090` | `pass` | The F5 BIG-IP appliance must enforce password complexity by requiring that at least one numeric character be used. | Password policy requires secure enforcement and at least one numeric character. |
| `V-266091` | `pass` | The F5 BIG-IP appliance must enforce password complexity by requiring that at least one special character be used. | Password policy requires secure enforcement and at least one special character. |
| `V-266092` | `pass` | The F5 BIG-IP appliance must require that when a password is changed, the characters are changed in at least eight of the positions within the password. | Password reuse distance was evaluated from sys db password.difok. |
| `V-266093` | `not-applicable` | The F5 BIG-IP appliance must prohibit the use of cached authenticators after eight hours or less. | Control applies only when device-management authentication uses ClientCert LDAP with cached OCSP settings. |
| `V-266094` | `not-applicable` | The F5 BIG-IP appliance must be configured to use DOD approved OCSP responders or CRLs to validate certificates used for PKI-based authentication. | Control applies only when device-management authentication uses ClientCert LDAP / PKI-based auth. |
| `V-266095` | `pass` | The F5 BIG-IP appliance must set the idle time before automatic logout to five minutes of inactivity except to fulfill documented and validated mission requirements. | Idle logout was evaluated across HTTPD/TMUI, CLI, console, and SSH timeout sources. |
| `V-266096` | `blocked-external` | The F5 BIG-IP appliance must conduct backups of the configuration at a weekly or organization-defined frequency and store on a separate device. | Local UCS archives are present, but off-device storage location and organization-defined backup frequency require external backup-policy evidence beyond the appliance snapshot. |

## Remaining Non-Pass Controls

### `fail`

- `V-266174` — The VPN Gateway must use Always On VPN connections for remote computing.: Always-on VPN posture was evaluated from the network-access resource and connectivity profile.
- `V-266083` — The F5 BIG-IP appliance must obtain its public key certificates from an appropriate certificate policy through an approved service provider.: Device certificate issuer was evaluated from the installed TMUI certificate; placeholder/self-signed local certificates are a finding.

### `not-applicable`

- `V-266139` — The F5 BIG-IP appliance providing intermediary services for remote access must use FIPS-validated cryptographic algorithms, including TLS 1.2 at a minimum.: If the BIG-IP appliance does not provide intermediary services for remote access (e.g., web content filter, TLS, and webmail), TLS, or application protocols that use TLS (e.g., DNSSEC or HTTPS), this is not applicable.
- `V-266153` — The F5 BIG-IP appliance must configure certification path validation to ensure revoked machine credentials are prohibited from establishing an allowed session.: If the BIG-IP appliance does not provide intermediary services for TLS, or application protocols that use TLS (e.g., DNSSEC or HTTPS), this is not applicable.
- `V-266165` — The F5 BIG-IP appliance must configure certificate path validation to ensure revoked user credentials are prohibited from establishing an allowed session.: If the BIG-IP appliance does not provide intermediary services for TLS, or application protocols that use TLS (e.g., DNSSEC or HTTPS), this is not applicable.
- `V-266093` — The F5 BIG-IP appliance must prohibit the use of cached authenticators after eight hours or less.: Control applies only when device-management authentication uses ClientCert LDAP with cached OCSP settings.
- `V-266094` — The F5 BIG-IP appliance must be configured to use DOD approved OCSP responders or CRLs to validate certificates used for PKI-based authentication.: Control applies only when device-management authentication uses ClientCert LDAP / PKI-based auth.

### `blocked-external`

- `V-266145` — The F5 BIG-IP appliance providing user access control intermediary services must display the Standard Mandatory DOD-approved Notice and Consent Banner before granting access to the network.: The access policy contains a message-box stage and a dedicated stig_dod_notice object, but the current API evidence does not expose the rendered banner body or branch order strongly enough to prove the exact DoD banner text before allow.
- `V-266150` — The F5 BIG-IP appliance must be configured to prohibit or restrict the use of unnecessary or prohibited functions, ports, protocols, and/or services, including those defined in the PPSM CAL and vulnerability assessments.: These controls depend on organization-approved service catalogs or ISSM/ISSO authorization inputs that cannot be derived from the appliance alone.
- `V-266154` — The F5 BIG-IP appliance providing user authentication intermediary services using PKI-based user authentication must implement a local cache of revocation data to support path discovery and validation in case of the inability to access revocation information via the network.: The access policy shows PKI-oriented stages, but the current evidence set does not expose fallback deny branches or local cached revocation behavior strongly enough to prove compliance.
- `V-266160` — The F5 BIG-IP appliance providing content filtering must detect use of network services that have not been authorized or approved by the information system security manager (ISSM) and information system security officer (ISSO), at a minimum.: These controls depend on organization-approved service catalogs or ISSM/ISSO authorization inputs that cannot be derived from the appliance alone.
- `V-266074` — The F5 BIG-IP appliance must manage local audit storage capacity in accordance with organization-defined audit record storage requirements.: Local log-storage capacity is observable on the appliance, but the STIG requires comparison against organization-defined retention and storage requirements from the SSP, which were not provided as live evidence.
- `V-266096` — The F5 BIG-IP appliance must conduct backups of the configuration at a weekly or organization-defined frequency and store on a separate device.: Local UCS archives are present, but off-device storage location and organization-defined backup frequency require external backup-policy evidence beyond the appliance snapshot.

