
{"cells": [{"cell_type": "markdown","metadata": {},"source": ["# V-266165 SQL-to-Pandas CPE Lifecycle\n","\n","Requirement: Validate user certificate revocation status in APM access policy\n","\n","This notebook reconstructs the carrier-law-comparison-pressure path from the validator artifacts.\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["from pathlib import Path\n","import json\n","import pandas as pd\n","\n","VULN_ID = "V-266165"\n","TARGET_HOSTNAME = None\n","TARGET_IP_ADDRESS = None\n","OUTPUT_DIR = None\n","for candidate in [Path.cwd().resolve(), Path.cwd().resolve().parent, Path("..").resolve()]:\n","    if (candidate / "domain_observations.jsonl").exists():\n","        OUTPUT_DIR = candidate\n","        break\n","if OUTPUT_DIR is None:\n","    raise FileNotFoundError("Could not locate domain_observations.jsonl")\n","\n","domain_observations_df = pd.read_json(OUTPUT_DIR / "domain_observations.jsonl", lines=True)\n","status_report_df = pd.read_csv(OUTPUT_DIR / "stig_status_report.csv")\n","formal_adjudications_df = pd.read_json(OUTPUT_DIR / "formal_adjudications.jsonl", lines=True)\n","execution_trace_df = pd.read_json(OUTPUT_DIR / "execution_trace.jsonl", lines=True)\n","with (OUTPUT_DIR / "resolved_contracts.json").open("r", encoding="utf-8") as handle:\n","    contracts_df = pd.DataFrame(json.load(handle).get("contracts", []))\n","\n","def _scope(frame):\n","    scoped = frame[frame["vuln_id"] == VULN_ID].copy()\n","    if TARGET_HOSTNAME:\n","        scoped = scoped[scoped["hostname"] == TARGET_HOSTNAME].copy()\n","    if TARGET_IP_ADDRESS:\n","        scoped = scoped[scoped["ip_address"] == TARGET_IP_ADDRESS].copy()\n","    if scoped.empty:\n","        raise ValueError("No scoped rows found")\n","    return scoped.sort_values([column for column in ["hostname", "ip_address", "run_timestamp"] if column in scoped.columns], kind="stable")\n","\n","obs_row = _scope(domain_observations_df).iloc[0].to_dict()\n","contract_row = contracts_df[contracts_df["vuln_id"] == VULN_ID].iloc[0].to_dict()\n","structured_evidence = dict(obs_row.get("structured_evidence") or {})\n","print("Selected host context:", {"hostname": obs_row.get("hostname"), "ip_address": obs_row.get("ip_address"), "run_timestamp": obs_row.get("run_timestamp")})\n","\n","def _rest_tables(row):\n","    tables = {}\n","    for endpoint, payload in (row.get("rest") or {}).items():\n","        if isinstance(payload, dict) and isinstance(payload.get("items"), list):\n","            tables[endpoint] = pd.DataFrame(payload.get("items", []))\n","        elif isinstance(payload, dict):\n","            tables[endpoint] = pd.DataFrame([payload]) if payload else pd.DataFrame()\n","        else:\n","            tables[endpoint] = pd.DataFrame()\n","    return tables\n","\n","def _tmsh_tables(row):\n","    tables = {}\n","    for command, payload in (row.get("tmsh") or {}).items():\n","        if isinstance(payload, list):\n","            tables[command] = pd.DataFrame(payload)\n","        elif isinstance(payload, dict):\n","            tables[command] = pd.DataFrame([payload])\n","        elif payload in [None, ""]:\n","            tables[command] = pd.DataFrame()\n","        else:\n","            tables[command] = pd.DataFrame([{"raw": str(payload)}])\n","    return tables\n","\n","def _trace_scope(frame):\n","    scoped = frame[frame["vuln_id"] == VULN_ID].copy()\n","    if TARGET_HOSTNAME:\n","        scoped = scoped[scoped["hostname"] == TARGET_HOSTNAME].copy()\n","    if TARGET_IP_ADDRESS:\n","        scoped = scoped[scoped["ip_address"] == TARGET_IP_ADDRESS].copy()\n","    return scoped.sort_values([column for column in ["hostname", "ip_address", "run_timestamp"] if column in scoped.columns], kind="stable")\n","\n","def _artifact_frame(artifact_name):\n","    scoped = _trace_scope(execution_trace_df)\n","    scoped = scoped[scoped["artifact"] == artifact_name].copy()\n","    rows = []\n","    for row in scoped.get("row", []):\n","        rows.append(row if isinstance(row, dict) else {})\n","    return pd.DataFrame(rows)\n","\n","REST_TABLES = _rest_tables(obs_row)\n","TMSH_TABLES = _tmsh_tables(obs_row)\n","\n","def _all_source_tables():\n","    rows = []\n","    for endpoint, frame in REST_TABLES.items():\n","        if frame.empty:\n","            rows.append(pd.DataFrame([{"source_endpoint": endpoint, "carrier_coordinate": f"{endpoint}::empty"}]))\n","            continue\n","        local = frame.copy()\n","        local["source_endpoint"] = endpoint\n","        if "carrier_coordinate" not in local.columns:\n","            if "fullPath" in local.columns:\n","                local["carrier_coordinate"] = local["fullPath"].astype(str)\n","            elif "name" in local.columns:\n","                local["carrier_coordinate"] = local["name"].astype(str)\n","            else:\n","                local["carrier_coordinate"] = [f"{endpoint}::row:{i}" for i in range(len(local))]\n","        rows.append(local)\n","    for command, frame in TMSH_TABLES.items():\n","        if frame.empty:\n","            rows.append(pd.DataFrame([{"source_endpoint": command, "carrier_coordinate": f"{command}::empty"}]))\n","            continue\n","        local = frame.copy()\n","        local["source_endpoint"] = command\n","        if "carrier_coordinate" not in local.columns:\n","            if "fullPath" in local.columns:\n","                local["carrier_coordinate"] = local["fullPath"].astype(str)\n","            elif "name" in local.columns:\n","                local["carrier_coordinate"] = local["name"].astype(str)\n","            else:\n","                local["carrier_coordinate"] = [f"{command}::row:{i}" for i in range(len(local))]\n","        rows.append(local)\n","    return pd.concat(rows, ignore_index=True, sort=False) if rows else pd.DataFrame()\n","\n","def _source_frame_for(lawful_source):\n","    if lawful_source in REST_TABLES:\n","        return REST_TABLES[lawful_source].copy()\n","    if lawful_source in TMSH_TABLES:\n","        return TMSH_TABLES[lawful_source].copy()\n","    for candidate in [part.strip() for part in str(lawful_source).split("+")]:\n","        if candidate in REST_TABLES:\n","            return REST_TABLES[candidate].copy()\n","        if candidate in TMSH_TABLES:\n","            return TMSH_TABLES[candidate].copy()\n","    return pd.DataFrame()\n","\n","PROPERTY_ALIASES = {\n","    "options": ["options", "tmOptions"],\n","    "tmOptions": ["tmOptions", "options"],\n","    "destination_port": ["destination_port", "destination"],\n","    "minimumLength": ["minimumLength", "minLength"],\n","    "statusAge": ["statusAge", "maxAge"],\n","    "partitionAccess": ["partitionAccess", "partition-access"],\n","    "partition-access": ["partitionAccess", "partition-access"],\n","    "guiSecurityBanner": ["guiSecurityBanner"],\n","    "guiSecurityBannerText": ["guiSecurityBannerText"],\n","    "value": ["value", "raw"],\n","    "include": ["include", "raw"],\n","    "logSettings": ["logSettings", "logSetting", "logSettingsReference", "logSettingReference"],\n","    "fwEnforcedPolicy": ["fwEnforcedPolicy", "fwEnforcedPolicyReference"],\n","    "addressSpaceExcludeSubnet": ["addressSpaceExcludeSubnet", "addressSpaceExcludeReference"],\n","    "addressSpaceExcludeDnsName": ["addressSpaceExcludeDnsName", "addressSpaceExcludeReference"],\n","    "ipv6AddressSpaceExcludeSubnet": ["ipv6AddressSpaceExcludeSubnet", "addressSpaceExcludeReference"],\n","    "asm_policies_active": ["active"],\n","    "virtual_server_security_policy": ["virtualServers"],\n","    "auto_update_check": ["autoCheck"],\n","    "live_update_realtime": ["frequency"],\n","    "classification": ["classification", "raw"],\n","    "publisher": ["publisher", "raw"],\n","}\n","\n","def _candidate_properties(lawful_property):\n","    return PROPERTY_ALIASES.get(str(lawful_property), [str(lawful_property)])\n","\n","def _materialize_carrier_frame(frame, source_name):\n","    local = frame.copy()\n","    if local.empty:\n","        return pd.DataFrame([{"source_endpoint": source_name, "carrier_coordinate": f"{source_name}::empty"}])\n","    if "source_endpoint" not in local.columns:\n","        local["source_endpoint"] = source_name\n","    if "carrier_coordinate" not in local.columns:\n","        if "fullPath" in local.columns:\n","            local["carrier_coordinate"] = local["fullPath"].astype(str)\n","        elif "name" in local.columns:\n","            local["carrier_coordinate"] = local["name"].astype(str)\n","        else:\n","            local["carrier_coordinate"] = [f"{source_name}::row:{i}" for i in range(len(local))]\n","    return local\n","\n","def _derive_observed_values(frame, lawful_property):\n","    local = _materialize_carrier_frame(frame, "derived")\n","    candidates = _candidate_properties(lawful_property)\n","    for candidate in candidates:\n","        if candidate in local.columns:\n","            return local[["carrier_coordinate"]].assign(observed_value=local[candidate], observed_property=candidate)\n","    if "destination" in local.columns and lawful_property == "destination_port":\n","        derived = local["destination"].astype(str).str.extract(r":(\d+)$")[0].astype("Int64")\n","        return local[["carrier_coordinate"]].assign(observed_value=derived, observed_property="destination -> destination_port")\n","    return pd.DataFrame(columns=["carrier_coordinate", "observed_value", "observed_property"])\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 1) O - Observation\n","\n","Pull back APM access profiles that have assigned access policies, then materialize referenced policy items as typed auth/MFA rows.\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["access_profiles_df = pd.DataFrame((obs_row.get("rest") or {}).get("/mgmt/tm/apm/profile/access", {}).get("items", []))\n","access_policies_df = pd.DataFrame((obs_row.get("rest") or {}).get("/mgmt/tm/apm/policy/access-policy", {}).get("items", []))\n","\n","def _infer_apm_item_metadata(item_name):\n","    lower = str(item_name or "").lower()\n","    mappings = [\n","        ("client_cert_inspection", "Client Cert Inspection", None),\n","        ("machinecert_auth", "Machine Cert Auth", "endpoint-check-machine-cert"),\n","        ("ocsp_auth", "OCSP Auth", "aaa-ocsp"),\n","        ("crldp_auth", "CRLDP Auth", "aaa-crldp"),\n","        ("tacacsplus_auth", "TACACS+ Auth", "aaa-tacacsplus"),\n","        ("kerberos_auth", "Kerberos Auth", "aaa-kerberos"),\n","        ("radius_auth", "RADIUS Auth", "aaa-radius"),\n","        ("ldap_auth", "LDAP Auth", "aaa-ldap"),\n","        ("active_directory", "Active Directory Auth", "aaa-active-directory"),\n","        ("ad_auth", "AD Auth", "aaa-active-directory"),\n","        ("rsa_aa_auth", "RSA AA Auth", "aaa-rsa-aa"),\n","        ("securid_auth", "SecurID Auth", "aaa-securid"),\n","        ("okta_mfa", "Okta MFA", "okta-mfa"),\n","        ("f5_mfa_user_verification", "F5 MFA User Verification", "aaa-f5-mfa-user-verification"),\n","    ]\n","    for marker, caption, agent_type in mappings:\n","        if marker in lower:\n","            agent_payload = {"certificateType": "user"} if marker == "ocsp_auth" else {}\n","            return {"caption": caption, "itemType": "action", "agent_types": [agent_type] if agent_type else [], "agent_payload": agent_payload}\n","    return {"caption": item_name, "itemType": None, "agent_types": [], "agent_payload": {}}\n","\n","selected_profiles_df = access_profiles_df.copy()\n","if not selected_profiles_df.empty:\n","    selected_profiles_df = selected_profiles_df[selected_profiles_df.get("scope").astype(str).str.lower().eq("profile")].copy()\n","    selected_profiles_df = selected_profiles_df[~selected_profiles_df.get("fullPath").isin(["/Common/kerberos_auth_config_default"])].copy()\n","    selected_profiles_df = selected_profiles_df[selected_profiles_df.get("accessPolicy").notna()].copy()\n","selected_profiles_df = selected_profiles_df[["fullPath","accessPolicy"]].rename(columns={"fullPath": "profile_path", "accessPolicy": "policy_path"}) if not selected_profiles_df.empty else pd.DataFrame(columns=["profile_path","policy_path"])\n","\n","carrier_rows = []\n","for _, profile_row in selected_profiles_df.iterrows():\n","    policy_path = profile_row.get("policy_path")\n","    matched_policy = access_policies_df[access_policies_df.get("fullPath") == policy_path]\n","    nested_items = matched_policy.iloc[0].get("items", []) if not matched_policy.empty else []\n","    if not nested_items:\n","        carrier_rows.append({"carrier_coordinate": f"{profile_row.get('profile_path')}#policy:none", "profile_path": profile_row.get("profile_path"), "policy_path": policy_path, "item_name": None, "caption": None, "itemType": None, "agent_types": [], "agent_payload": {}})\n","        continue\n","    for item in nested_items:\n","        item_name = item.get("name") if isinstance(item, dict) else None\n","        metadata =*infer_apm_item_metadata(item_name)\n",**"        carrier_rows.append({"carrier_coordinate": f"{profile_row.get('profile_path')}#{item_name}", "profile_path": profile_row.get("profile_path"), "policy_path": policy_path, "item_name": item_name, "caption": metadata["caption"], "itemType": metadata["itemType"], "agent_types": metadata["agent_types"], "agent_payload": metadata["agent_payload"]})\n",**"carrier_df = pd.DataFrame(carrier_rows)\n",**"carrier_df\n"**]**},**{**"cell_type": "markdown",**"metadata": {},**"source": [**"## 2) K - Schema\n",**"\n",**"Make the law inspectable by listing each required auth/MFA group and the admissible policy-item signatures that satisfy it.\n"**]**},**{**"cell_type": "markdown",**"metadata": {},**"source": [**"## 2a) K Preview - Embedded Law Rows\n",**"\n",**"Static law preview embedded by the generator so the lawful structure is visible even before notebook execution.\n",**"\n",**"| carrier_object | lawful_property | admissible_constraint | lawful_source |\n",**"|---|---|---|---|\n",**"| APM access policy profile | required_policy_group_1 | at least one policy item must match group 1 | /mgmt/tm/apm/profile/access |\n",**"| APM access policy profile | required_policy_group_2 | at least one policy item must match group 2 | /mgmt/tm/apm/profile/access |\n"**]**},**{**"cell_type": "code",**"execution_count": null,**"metadata": {},**"outputs": [],**"source": [**"lawful_structure_rows = []\n",**"for group_index, group in enumerate([[{"caption_aliases": ["Client Cert Inspection"], "item_type_aliases": ["action"]}], [{"caption_aliases": ["OCSP Auth", "OCSP Authentication"], "item_type_aliases": ["action"], "agent_type_aliases": ["aaa-ocsp"], "agent_payload_match": {"certificateType": "user"}}, {"caption_aliases": ["CRLDP Auth", "CRLDP Authentication"], "item_type_aliases": ["action"], "agent_type_aliases": ["aaa-crldp"]}]], start=1):\n",**"    for spec in group:\n",**"        lawful_structure_rows.append({\n",**"            "carrier_object": "APM access policy profile",\n",**"            "lawful_property": f"required_policy_group*{group_index}",\n","            "admissible_constraint": f"at least one policy item must match required group {group_index}",\n","            "expected_value": spec,\n","            "lawful_source": "/mgmt/tm/apm/profile/access + /mgmt/tm/apm/policy/access-policy",\n","        })\n","lawful_structure_df = pd.DataFrame(lawful_structure_rows)\n","lawful_structure_df["lawful_signature_summary"] = lawful_structure_df["expected_value"].apply(lambda spec: {\n","    "caption_aliases": spec.get("caption_aliases", []),\n","    "item_type_aliases": spec.get("item_type_aliases", []),\n","    "agent_type_aliases": spec.get("agent_type_aliases", []),\n","})\n","lawful_structure_df\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 3) C1 - Candidate Pullback\n","\n","Show the observed policy-item candidates before any law reduction.\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["candidate_df = carrier_df[["carrier_coordinate","profile_path","policy_path","item_name","caption","itemType","agent_types","agent_payload"]].copy()\n","candidate_df\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 3) C2 - Alignment\n","\n","For each observed policy item and each lawful group signature, show how the alignment attempt succeeds or fails field by field.\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["def _policy_item_matches_spec(item_row, spec):\n","    caption_aliases = [str(v) for v in spec.get("caption_aliases", [])]\n","    item_type_aliases = [str(v) for v in spec.get("item_type_aliases", [])]\n","    agent_type_aliases = [str(v) for v in spec.get("agent_type_aliases", [])]\n","    caption = str(item_row.get("caption") or "")\n","    item_type = str(item_row.get("itemType") or "")\n","    agent_types = [str(v) for v in (item_row.get("agent_types") or [])]\n","    observed_payload = item_row.get("agent_payload") or {}\n","    if caption_aliases and caption not in caption_aliases:\n","        return False\n","    if item_type_aliases and item_type not in item_type_aliases:\n","        return False\n","    if agent_type_aliases and not any(agent in agent_type_aliases for agent in agent_types):\n","        return False\n","    payload_match_spec = spec.get("agent_payload_match", {}) if isinstance(spec.get("agent_payload_match"), dict) else {}\n","    for key, expected in payload_match_spec.items():\n","        if observed_payload.get(key) != expected:\n","            return False\n","    return True\n","\n","alignment_rows = []\n","for profile_path, profile_items in carrier_df.groupby("profile_path", dropna=True):\n","    for group_index, group in enumerate([[{"caption_aliases": ["Client Cert Inspection"], "item_type_aliases": ["action"]}], [{"caption_aliases": ["OCSP Auth", "OCSP Authentication"], "item_type_aliases": ["action"], "agent_type_aliases": ["aaa-ocsp"], "agent_payload_match": {"certificateType": "user"}}, {"caption_aliases": ["CRLDP Auth", "CRLDP Authentication"], "item_type_aliases": ["action"], "agent_type_aliases": ["aaa-crldp"]}]], start=1):\n","        for _, item_row in profile_items.iterrows():\n","            for spec_index, spec in enumerate(group, start=1):\n","                caption_aliases = [str(v) for v in spec.get("caption_aliases", [])]\n","                item_type_aliases = [str(v) for v in spec.get("item_type_aliases", [])]\n","                agent_type_aliases = [str(v) for v in spec.get("agent_type_aliases", [])]\n","                caption = str(item_row.get("caption") or "")\n","                item_type = str(item_row.get("itemType") or "")\n","                agent_types = [str(v) for v in (item_row.get("agent_types") or [])]\n","                observed_payload = item_row.get("agent_payload") or {}\n","                payload_match_spec = spec.get("agent_payload_match", {}) if isinstance(spec.get("agent_payload_match"), dict) else {}\n","                caption_match = (not caption_aliases) or (caption in caption_aliases)\n","                item_type_match = (not item_type_aliases) or (item_type in item_type_aliases)\n","                agent_type_match = (not agent_type_aliases) or any(agent in agent_type_aliases for agent in agent_types)\n","                payload_match = all(observed_payload.get(key) == expected for key, expected in payload_match_spec.items())\n","                overall_match = caption_match and item_type_match and agent_type_match and payload_match\n","                alignment_rows.append({\n","                    "profile_path": profile_path,\n","                    "group_index": group_index,\n","                    "spec_index": spec_index,\n","                    "carrier_coordinate": item_row.get("carrier_coordinate"),\n","                    "item_name": item_row.get("item_name"),\n","                    "observed_caption": caption,\n","                    "observed_item_type": item_type,\n","                    "observed_agent_types": agent_types,\n","                    "required_caption_aliases": caption_aliases,\n","                    "required_item_type_aliases": item_type_aliases,\n","                    "required_agent_type_aliases": agent_type_aliases,\n","                    "required_agent_payload_match": payload_match_spec,\n","                    "caption_match": caption_match,\n","                    "item_type_match": item_type_match,\n","                    "agent_type_match": agent_type_match,\n","                    "observed_agent_payload": observed_payload,\n","                    "agent_payload_match": payload_match,\n","                    "overall_match": overall_match,\n","                })\n","alignment_df = pd.DataFrame(alignment_rows)\n","alignment_df\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 3) C3 - Comparison\n","\n","Reduce the alignment rows to the group-level judgment, but keep the successful witnesses visible.\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["comparison_rows = []\n","for (profile_path, group_index), group_rows in alignment_df.groupby(["profile_path", "group_index"], dropna=True):\n","    required_caption_aliases = sorted({value for row in group_rows["required_caption_aliases"] for value in row})\n","    required_item_type_aliases = sorted({value for row in group_rows["required_item_type_aliases"] for value in row})\n","    required_agent_type_aliases = sorted({value for row in group_rows["required_agent_type_aliases"] for value in row})\n","    required_agent_payload_match = {}\n","    for payload_map in group_rows["required_agent_payload_match"]:\n","        for key, value in payload_map.items():\n","            required_agent_payload_match[str(key)] = value\n","    caption_hits = group_rows[group_rows["caption_match"]]\n","    item_type_hits = group_rows[group_rows["item_type_match"]]\n","    agent_type_hits = group_rows[group_rows["agent_type_match"]]\n","    payload_hits = group_rows[group_rows["agent_payload_match"]]\n","    overall_hits = group_rows[group_rows["overall_match"]]\n","    comparison_rows.extend([\n","        {\n","            "carrier_coordinate": f"{profile_path}#group:{group_index}",\n","            "measurable": "caption",\n","            "required": required_caption_aliases,\n","            "observed": sorted(caption_hits["observed_caption"].dropna().astype(str).unique().tolist()),\n","            "match": bool(not required_caption_aliases or not overall_hits.empty),\n","        },\n","        {\n","            "carrier_coordinate": f"{profile_path}#group:{group_index}",\n","            "measurable": "itemType",\n","            "required": required_item_type_aliases,\n","            "observed": sorted(item_type_hits["observed_item_type"].dropna().astype(str).unique().tolist()),\n","            "match": bool(not required_item_type_aliases or not overall_hits.empty),\n","        },\n","        {\n","            "carrier_coordinate": f"{profile_path}#group:{group_index}",\n","            "measurable": "agent_types",\n","            "required": required_agent_type_aliases,\n","            "observed": sorted({value for values in agent_type_hits["observed_agent_types"] for value in values}),\n","            "match": bool(not required_agent_type_aliases or not overall_hits.empty),\n","        },\n","    ])\n","    for payload_key, payload_value in required_agent_payload_match.items():\n","        comparison_rows.append(\n","            {\n","                "carrier_coordinate": f"{profile_path}#group:{group_index}",\n","                "measurable": f"agent_payload.{payload_key}",\n","                "required": [str(payload_value)],\n","                "observed": sorted(\n","                    {\n","                        str(payload_map.get(payload_key))\n","                        for payload_map in payload_hits["observed_agent_payload"]\n","                        if isinstance(payload_map, dict) and payload_map.get(payload_key) is not None\n","                    }\n","                ),\n","                "match": bool(not required_agent_payload_match or not overall_hits.empty),\n","            }\n","        )\n","comparison_df = pd.DataFrame(comparison_rows)\n","comparison_df\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 4) Lambda - Pressure\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["pressure_df = comparison_df[["carrier_coordinate","measurable"]].copy()\n","pressure_df["pressure_magnitude"] = (~comparison_df["match"]).astype(float)\n","pressure_df["pressure_cause"] = comparison_df["match"].apply(lambda ok: "required policy-item group measurable satisfied" if ok else "required policy-item group measurable missing")\n","pressure_df\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 5) Sigma - Strategy\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["strategy_df = _artifact_frame("strategy_df")\n","if strategy_df.empty and not pressure_df.empty:\n","    strategy_df = pressure_df.copy()\n","    strategy_df["candidate_interventions"] = strategy_df["pressure_magnitude"].apply(lambda value: ["REMEDIATE", "DEFER_AND_RECOLLECT"] if value > 0 else ["NO_OP", "MONITOR"])\n","    strategy_df["selected_intervention"] = strategy_df["pressure_magnitude"].apply(lambda value: "REMEDIATE" if value > 0 else "NO_OP")\n","    strategy_df["selection_rationale"] = strategy_df["pressure_cause"]\n","strategy_df\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 6) M - Realization\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["realization_df = _artifact_frame("realization_df")\n","if realization_df.empty and not strategy_df.empty:\n","    realization_df = strategy_df[["carrier_coordinate","selected_intervention"]].copy()\n","    realization_df["lawful_channel"] = contract_row.get("runtime_family")\n","    realization_df["verification_plan"] = "re-observe carrier and rebuild comparison/pressure"\n","realization_df\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 7) W - Work\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["work_df = _artifact_frame("work_df")\n","if work_df.empty:\n","    work_df = pd.DataFrame([{"inspection_work_units": len(locals().get("carrier_df", pd.DataFrame())), "comparison_work_units": len(comparison_df), "mutation_work_units": int((strategy_df["selected_intervention"] != "NO_OP").sum()), "verification_work_units": len(pressure_df)}])\n","work_df\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 8) V - Value\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["value_df = _artifact_frame("value_df")\n","if value_df.empty:\n","    value_df = pd.DataFrame([{"computed_status": "NOT_A_FINDING" if float(pressure_df["pressure_magnitude"].sum()) == 0.0 else "OPEN", "residual_pressure": float(pressure_df["pressure_magnitude"].sum())}])\n","value_df\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 9) P - Persistence\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["persistence_df = _artifact_frame("persistence_df")\n","if persistence_df.empty and not pressure_df.empty:\n","    persistence_df = pressure_df.copy()\n","    persistence_df["lineage_path"] = "carrier_df -> schema_df -> comparison_df -> pressure_df"\n","persistence_df\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 10) J - Self-Index\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["self_index_df = _artifact_frame("self_index_df")\n","if self_index_df.empty and not pressure_df.empty:\n","    self_index_df = pressure_df.copy()\n","    self_index_df["discrepancy_motif"] = self_index_df["pressure_cause"]\n","    self_index_df["next_cycle_hint"] = self_index_df["pressure_magnitude"].apply(lambda value: "prioritize this coordinate" if value > 0 else "monitor only")\n","self_index_df\n"]},{"cell_type": "markdown","metadata": {},"source": ["## 11) E - Error\n"]},{"cell_type": "code","execution_count": null,"metadata": {},"outputs": [],"source": ["error_df = _artifact_frame("error_df")\n","if error_df.empty and not pressure_df.empty:\n","    error_df = pressure_df.copy()\n","    error_df["resolution_state"] = error_df["pressure_magnitude"].apply(lambda value: "deficit_cleared" if value == 0 else "residual_pressure_present")\n","    error_df["next_corrective_action"] = error_df["resolution_state"].apply(lambda value: "no residual mismatch" if value == "deficit_cleared" else "retry realization or challenge law/runtime assumptions")\n","error_df\n"]}],"metadata": {"kernelspec": {"display_name": "Python 3","language": "python","name": "python3"},"language_info": {"name": "python","version": "3.x"}},"nbformat": 4,"nbformat_minor": 5}